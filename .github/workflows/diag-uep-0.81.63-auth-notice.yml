name: Diagnose UEP 0.81.63 Auth Notice
'on':
  push:
    branches: [main]
    paths: [diag-uep-0.81.63-auth-notice.trigger]
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: windows-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Download 0.81.63
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference='Stop';New-Item -ItemType Directory -Force source,app|Out-Null
          gh release download v0.81.63 --repo $env:GITHUB_REPOSITORY --pattern '*.zip' --dir source
          $z=Get-ChildItem source -File -Filter '*.zip'|Select-Object -First 1;if(!$z){throw '0.81.63 zip missing'};Expand-Archive $z.FullName app -Force
      - name: Inspect
        shell: pwsh
        run: |
          node scripts/diag-uep-0.81.63-auth-notice.js app
          node scripts/diag-uep-0.81.63-exact.js app
      - name: Commit diagnostic
        shell: pwsh
        run: |
          git config user.name 'UEP Diagnostics'; git config user.email 'actions@github.com'
          git add diag-08163-auth-notice.txt diag-08163-exact.txt
          git commit -m 'diag: capture 0.81.63 exact auth and notice paths' || exit 0
          git push
