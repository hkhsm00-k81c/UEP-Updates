const fs=require('fs');
const path=require('path');
const root=process.argv[2]||'app/resources/app';
const g=fs.readFileSync(path.join(root,'gyomuon.js'),'utf8');
const m=fs.readFileSync(path.join(root,'electron','main.cjs'),'utf8');
const d=fs.readFileSync(path.join(root,'electron','google-data.cjs'),'utf8');
const p=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));
const ok=(cond,msg)=>{if(!cond)throw new Error(msg);console.log('PASS',msg)};
ok(p.version==='0.82.70','package version 0.82.70');
ok(g.includes('const APP_VERSION = "0.82.70";'),'renderer version 0.82.70');
ok(m.includes("readSheetBatch(token,UEP_PROCESSING_SPREADSHEET_ID,[\"'30_야자출결_정규화'!A:U\"]"),'operational refresh reads normalized night attendance');
ok(m.includes("matrices['30_야자출결_정규화']=nightResult?.[0]?.values||[]"),'night matrix is parsed through canonical parser');
ok(m.includes('nightAttendance:partial.nightAttendance||[]'),'operational patch updates nightAttendance cache');
ok(m.includes('const operationalSyncedAt=new Date().toISOString();liveDataCache={...liveDataCache,...patch,operationalSyncedAt}'),'existing operational cache merge preserved');
ok(m.includes("const UEP_OPERATIONAL_RANGE_NAMES=Object.freeze(['30_공식출결기록','31_지각기록','40_공지마감','44_공지확인현황','42_급식지도계획','43_야자감독계획'])"),'existing operational sources preserved');
ok(d.includes('sessionNo:computedSessionNo.get(sessionId)'),'0.82.68 session-number behavior preserved');
ok(g.includes('uep-uni-min-track-list'),'0.82.69 university counseling UI preserved');
ok(!m.includes('MutationObserver'),'main process has no DOM patch');
console.log('UEP 0.82.70 night attendance live refresh regression OK');
