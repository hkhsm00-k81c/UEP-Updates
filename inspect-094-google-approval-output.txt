=== POLICY ===
clientId present=True
clientId desktop-like=True
clientId length=72

=== MAIN TERM: loadGoogleOAuthClientId ===
count=3
k`;
  try { await fs.rename(filePath, backup); } catch {}
  const error = new Error("저장된 보안 인증정보를 복호화할 수 없습니다. 서비스 계정 JSON을 다시 등록해 주세요.");
  error.code = "UEP_SAFE_STORAGE_DECRYPT_FAILED";
  throw error;
}

function base64url(value) { return Buffer.from(value).toString("base64url"); }

// __UEP_GOOGLE_USER_OAUTH_08070__
const UEP_UPDATE_POLICY_URL = "https://raw.githubusercontent.com/hkhsm00-k81c/UEP-Updates/main/uep-policy.json";
let googleOAuthPolicyCache={clientId:"",expiresAt:0};

function googleOAuthError(message,code){const e=new Error(message);e.code=code;return e;}
async function loadGoogleOAuthClientId(){
  if(googleOAuthPolicyCache.clientId && googleOAuthPolicyCache.expiresAt>Date.now()) return googleOAuthPolicyCache.clientId;
  let response;
  try{ response=await net.fetch(UEP_UPDATE_POLICY_URL,{cache:"no-store"}); }
  catch(error){ throw googleOAuthError("Google 계정 연결 설정을 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.","UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE"); }
  if(!response.ok) throw googleOAuthError(`Google 계정 연결 설정 조회 실패 (${response.status})`,"UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE");
  const policy=await response.json();
  const clientId=String(policy?.googleOAuthClientId||"").trim();
  if(!/\.apps\.googleusercontent\.com$/i.test(clientId)) throw googleOAuthError("관리자가 UEP Google OAuth 클라이언트를 아직 설정하지 않았습니다.","UEP_GOOGLE_OAUTH_CLIENT_NOT_CONFIGURED");
  googleOAuthPolicyCache={clientId,expiresAt:Date.now()+10*60*1000};
  return clientId;
}
function googleUserOAuthR
---
eGoogleUserOAuth(recovered);return recovered;}
  }catch{}
  return null;
}
async function googleUserInfo(accessToken){
  const response=await net.fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{authorization:`Bearer ${accessToken}`}});
  if(!response.ok) throw googleOAuthError(`Google 사용자 확인 실패 (${response.status})`,'UEP_GOOGLE_OAUTH_USERINFO_FAILED');
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(6
---
{token:await getSheetsToken(service),mode:'service_account',account:service.client_email||''};
  }catch(error){
    const fallbackAllowed=error?.code==='UEP_GOOGLE_CREDENTIAL_MISSING'||error?.code==='ENOENT'||error?.code==='UEP_SAFE_STORAGE_DECRYPT_FAILED'||/no such file/i.test(String(error?.message||''));
    if(!fallbackAllowed) throw error;
  }
  const saved=await readGoogleUserOAuth();
  const token=await getGoogleUserSheetsToken();
  return {token,mode:'user_oauth',account:String(saved?.email||'')};
}
async function authorizeGoogleUser({loginHint=''}={}){
  const clientId=await loadGoogleOAuthClientId();
  const verifier=crypto.randomBytes(48).toString('base64url');
  const challenge=crypto.createHash('sha256').update(verifier).digest('base64url');
  const state=crypto.randomBytes(24).toString('base64url');
  let timer=null;
  let settled=false;
  let callbackResolve,callbackReject;
  const callbackPromise=new Promise((resolve,reject)=>{callbackResolve=resolve;callbackReject=reject;});
  const server=http.createServer((req,res)=>{
    try{
      const u=new URL(req.url||'/', 'http://127.0.0.1');
      if(u.pathname!=='/' && u.pathname!=='/oauth2callback'){res.writeHead(404);res.end('Not found');return;}
      const returnedState=String(u.searchParams.get('state')||'');
      const code=String(u.searchParams.get('code')||'');
      const error=String(u.searchParams.get('error')||'');
      res.writeHead(200,{'content-type':'text/html; charset=utf-
---
=== MAIN TERM: googleOAuthClientId ===
count=4
  try { await fs.rename(filePath, backup); } catch {}
  const error = new Error("저장된 보안 인증정보를 복호화할 수 없습니다. 서비스 계정 JSON을 다시 등록해 주세요.");
  error.code = "UEP_SAFE_STORAGE_DECRYPT_FAILED";
  throw error;
}

function base64url(value) { return Buffer.from(value).toString("base64url"); }

// __UEP_GOOGLE_USER_OAUTH_08070__
const UEP_UPDATE_POLICY_URL = "https://raw.githubusercontent.com/hkhsm00-k81c/UEP-Updates/main/uep-policy.json";
let googleOAuthPolicyCache={clientId:"",expiresAt:0};

function googleOAuthError(message,code){const e=new Error(message);e.code=code;return e;}
async function loadGoogleOAuthClientId(){
  if(googleOAuthPolicyCache.clientId && googleOAuthPolicyCache.expiresAt>Date.now()) return googleOAuthPolicyCache.clientId;
  let response;
  try{ response=await net.fetch(UEP_UPDATE_POLICY_URL,{cache:"no-store"}); }
  catch(error){ throw googleOAuthError("Google 계정 연결 설정을 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.","UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE"); }
  if(!response.ok) throw googleOAuthError(`Google 계정 연결 설정 조회 실패 (${response.status})`,"UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE");
  const policy=await response.json();
  const clientId=String(policy?.googleOAuthClientId||"").trim();
  if(!/\.apps\.googleusercontent\.com$/i.test(clientId)) throw googleOAuthError("관리자가 UEP Google OAuth 클라이언트를 아직 설정하지 않았습니다.","UEP_GOOGLE_OAUTH_CLIENT_NOT_CONFIGURED");
  googleOAuthPolicyCache={clientId,expiresAt:Date.now()+10*60*1000};
  return clientId;
}
function googleUserOAuthRecov
---
n e;}
async function loadGoogleOAuthClientId(){
  if(googleOAuthPolicyCache.clientId && googleOAuthPolicyCache.expiresAt>Date.now()) return googleOAuthPolicyCache.clientId;
  let response;
  try{ response=await net.fetch(UEP_UPDATE_POLICY_URL,{cache:"no-store"}); }
  catch(error){ throw googleOAuthError("Google 계정 연결 설정을 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.","UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE"); }
  if(!response.ok) throw googleOAuthError(`Google 계정 연결 설정 조회 실패 (${response.status})`,"UEP_GOOGLE_OAUTH_POLICY_UNAVAILABLE");
  const policy=await response.json();
  const clientId=String(policy?.googleOAuthClientId||"").trim();
  if(!/\.apps\.googleusercontent\.com$/i.test(clientId)) throw googleOAuthError("관리자가 UEP Google OAuth 클라이언트를 아직 설정하지 않았습니다.","UEP_GOOGLE_OAUTH_CLIENT_NOT_CONFIGURED");
  googleOAuthPolicyCache={clientId,expiresAt:Date.now()+10*60*1000};
  return clientId;
}
function googleUserOAuthRecoveryKey(salt){
  const identity=`${os.hostname()}|${os.userInfo().username}|UEP-GOOGLE-USER-OAUTH-v1`;
  return crypto.scryptSync(identity,salt,32);
}
function encodeGoogleUserOAuthRecovery(value){
  const salt=crypto.randomBytes(16),iv=crypto.randomBytes(12),key=googleUserOAuthRecoveryKey(salt);
  const cipher=crypto.createCipheriv('aes-256-gcm',key,iv);
  const encrypted=Buffer.concat([cipher.update(JSON.stringify(value),'utf8'),cipher.final()]);
  return {version:1,algorithm:'aes-256-gcm',salt:salt.toString('base64'),iv:iv.toString('base64'),tag:cipher.getAu
---
gleUserOAuth(recovered);return recovered;}
  }catch{}
  return null;
}
async function googleUserInfo(accessToken){
  const response=await net.fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{authorization:`Bearer ${accessToken}`}});
  if(!response.ok) throw googleOAuthError(`Google 사용자 확인 실패 (${response.status})`,'UEP_GOOGLE_OAUTH_USERINFO_FAILED');
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(60,Nu
---
en:await getSheetsToken(service),mode:'service_account',account:service.client_email||''};
  }catch(error){
    const fallbackAllowed=error?.code==='UEP_GOOGLE_CREDENTIAL_MISSING'||error?.code==='ENOENT'||error?.code==='UEP_SAFE_STORAGE_DECRYPT_FAILED'||/no such file/i.test(String(error?.message||''));
    if(!fallbackAllowed) throw error;
  }
  const saved=await readGoogleUserOAuth();
  const token=await getGoogleUserSheetsToken();
  return {token,mode:'user_oauth',account:String(saved?.email||'')};
}
async function authorizeGoogleUser({loginHint=''}={}){
  const clientId=await loadGoogleOAuthClientId();
  const verifier=crypto.randomBytes(48).toString('base64url');
  const challenge=crypto.createHash('sha256').update(verifier).digest('base64url');
  const state=crypto.randomBytes(24).toString('base64url');
  let timer=null;
  let settled=false;
  let callbackResolve,callbackReject;
  const callbackPromise=new Promise((resolve,reject)=>{callbackResolve=resolve;callbackReject=reject;});
  const server=http.createServer((req,res)=>{
    try{
      const u=new URL(req.url||'/', 'http://127.0.0.1');
      if(u.pathname!=='/' && u.pathname!=='/oauth2callback'){res.writeHead(404);res.end('Not found');return;}
      const returnedState=String(u.searchParams.get('state')||'');
      const code=String(u.searchParams.get('code')||'');
      const error=String(u.searchParams.get('error')||'');
      res.writeHead(200,{'content-type':'text/html; charset=utf-8','
---
=== MAIN TERM: accounts.google.com/o/oauth2 ===
count=1
</h2><p>이 창을 닫고 UEP로 돌아가 연결 완료 여부를 확인해 주세요.</p>');
      if(!settled){settled=true;callbackResolve(code);}
    }catch(error){if(!settled){settled=true;callbackReject(error);}}
  });
  try{
    await new Promise((resolve,reject)=>{server.once('error',reject);server.listen(0,'127.0.0.1',resolve);});
    const address=server.address();
    const port=typeof address==='object'&&address?address.port:0;
    if(!port) throw googleOAuthError('Google 승인용 로컬 포트를 열지 못했습니다.','UEP_GOOGLE_OAUTH_CALLBACK_FAILED');
    const redirectUri=`http://127.0.0.1:${port}`;
    const authUrl=new URL('https://accounts.google.com/o/oauth2/v2/auth');
    authUrl.searchParams.set('client_id',clientId);
    authUrl.searchParams.set('redirect_uri',redirectUri);
    authUrl.searchParams.set('response_type','code');
    authUrl.searchParams.set('scope','openid email https://www.googleapis.com/auth/spreadsheets.readonly');
    authUrl.searchParams.set('code_challenge',challenge);
    authUrl.searchParams.set('code_challenge_method','S256');
    authUrl.searchParams.set('state',state);
    authUrl.searchParams.set('access_type','offline');
    authUrl.searchParams.set('prompt','consent');
    if(String(loginHint||'').includes('@')) authUrl.searchParams.set('login_hint',String(loginHint).trim());
    await shell.openExternal(authUrl.toString());
    timer=setTimeout(()=>{if(!settled){settled=true;callbackReject(googleOAuthError('Google 계정 승인이 시간 초과되었습니다.','UEP_GOOGLE_OAUTH_TIMEOUT'));}},180
---
=== MAIN TERM: oauth2.googleapis.com/token ===
count=3
시각"],row["종료시각"],row["종일"]||(!row["시작시각"]?"Y":"N"),row["일정명"]||row["프로그램명"],row["상세내용"]||row["비고"],row["일정구분"]||"업무",row["대상"],row["장소"],row["부서"],row["담당자"],row["상태"]||"확정",row["검증상태"]||"정상",row["출처"]||"UEP 직접입력",row["원본시작셀"],row["원본끝셀"],row["원본일정ID"],row["이관방식"]||"직접기록",row["수정일시"],row["비고"]];
}


function validateServiceAccount(value) {
  return Boolean(
    value && value.type === "service_account" &&
    typeof value.client_email === "string" &&
    typeof value.private_key === "string" &&
    value.private_key.startsWith("-----BEGIN PRIVATE KEY-----") &&
    value.token_uri === "https://oauth2.googleapis.com/token"
  );
}

function recoveryKey(salt) {
  // safeStorage가 Windows/Chromium 키 문제로 복호화에 실패할 때만 사용하는 로컬 복구키입니다.
  // 동일 Windows 사용자/PC에서 재부팅 후에도 안정적으로 재생성됩니다.
  const identity = `${os.hostname()}|${os.userInfo().username}|UEP-CREDENTIAL-RECOVERY-v1`;
  return crypto.scryptSync(identity, salt, 32);
}

async function writeCredentialRecovery(value) {
  const salt = crypto.randomBytes(16), iv = crypto.randomBytes(12);
  const key = recoveryKey(salt);
  const cipher = crypto.createCipheriv("aes-256-gcm", key, iv);
  const encrypted = Buffer.concat([cipher.update(JSON.stringify(value), "utf8"), cipher.final()]);
  const payload = {
    version: 1, algorithm: "aes-256-gcm",
    salt: salt.toString("base64"), iv: iv.toString("base64"),
    tag: cipher.getAuthTag().toString("base64"), data: encrypted.toString("base64")
  };
  await fs.mkdir(path.dirname(credentialRecoveryPa
---
GoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(60,Number(next.expires_in||3600))*1000,updated_at:new Date().toISOString()};
  await saveGoogleUserOAuth(merged);
  return merged;
}
async function getGoogleUserSheetsToken(){
  let saved=await readGoogleUserOAuth();
  if(!saved) throw googleOAuthError("Google 계정 연결이 필요합니다.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  if(saved.access_token && Number(saved.expires_at||0)>Date.now()+90000) return String(saved.access_token);
  saved=await
---
s('@')) authUrl.searchParams.set('login_hint',String(loginHint).trim());
    await shell.openExternal(authUrl.toString());
    timer=setTimeout(()=>{if(!settled){settled=true;callbackReject(googleOAuthError('Google 계정 승인이 시간 초과되었습니다.','UEP_GOOGLE_OAUTH_TIMEOUT'));}},180000);
    const code=await callbackPromise;
    const body=new URLSearchParams();
    body.set('client_id',clientId);

    body.set('code',code);
    body.set('code_verifier',verifier);
    body.set('redirect_uri',redirectUri);
    body.set('grant_type','authorization_code');
    const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded;charset=UTF-8','accept':'application/json'},body:body.toString()});
    if(!response.ok){
      let errCode='',detail='',raw='';
      try{const payload=await response.json();errCode=String(payload?.error||'');detail=String(payload?.error_description||'');raw=JSON.stringify(payload);}catch{try{raw=await response.text();}catch{}}
      const suffix=[errCode,detail].filter(Boolean).join(' · ');
      throw googleOAuthError(`Google 토큰 교환 실패 (${response.status})${suffix?`: ${suffix}`:''}${!suffix&&raw?`: ${raw}`:''}`,'UEP_GOOGLE_OAUTH_TOKEN_FAILED');
    }
    const tokenPayload=await response.json();
    if(!tokenPayload.access_token) throw googleOAuthError('Google 액세스 토큰을 받지 못했습니다.','UEP_GOOGLE_OAUTH_TOKEN_FAILED');
    let info={};
    try{info=await googleUserInfo(tokenPayload
---
=== MAIN TERM: code_verifier ===
count=1
set('state',state);
    authUrl.searchParams.set('access_type','offline');
    authUrl.searchParams.set('prompt','consent');
    if(String(loginHint||'').includes('@')) authUrl.searchParams.set('login_hint',String(loginHint).trim());
    await shell.openExternal(authUrl.toString());
    timer=setTimeout(()=>{if(!settled){settled=true;callbackReject(googleOAuthError('Google 계정 승인이 시간 초과되었습니다.','UEP_GOOGLE_OAUTH_TIMEOUT'));}},180000);
    const code=await callbackPromise;
    const body=new URLSearchParams();
    body.set('client_id',clientId);

    body.set('code',code);
    body.set('code_verifier',verifier);
    body.set('redirect_uri',redirectUri);
    body.set('grant_type','authorization_code');
    const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded;charset=UTF-8','accept':'application/json'},body:body.toString()});
    if(!response.ok){
      let errCode='',detail='',raw='';
      try{const payload=await response.json();errCode=String(payload?.error||'');detail=String(payload?.error_description||'');raw=JSON.stringify(payload);}catch{try{raw=await response.text();}catch{}}
      const suffix=[errCode,detail].filter(Boolean).join(' · ');
      throw googleOAuthError(`Google 토큰 교환 실패 (${response.status})${suffix?`: ${suffix}`:''}${!suffix&&raw?`: ${raw}`:''}`,'UEP_GOOGLE_OAUTH_TOKEN_FAILED');
    }
    const tokenPayload=await response.json();
    if(!tokenPayload
---
=== MAIN TERM: redirect_uri ===
count=2
r){if(!settled){settled=true;callbackReject(error);}}
  });
  try{
    await new Promise((resolve,reject)=>{server.once('error',reject);server.listen(0,'127.0.0.1',resolve);});
    const address=server.address();
    const port=typeof address==='object'&&address?address.port:0;
    if(!port) throw googleOAuthError('Google 승인용 로컬 포트를 열지 못했습니다.','UEP_GOOGLE_OAUTH_CALLBACK_FAILED');
    const redirectUri=`http://127.0.0.1:${port}`;
    const authUrl=new URL('https://accounts.google.com/o/oauth2/v2/auth');
    authUrl.searchParams.set('client_id',clientId);
    authUrl.searchParams.set('redirect_uri',redirectUri);
    authUrl.searchParams.set('response_type','code');
    authUrl.searchParams.set('scope','openid email https://www.googleapis.com/auth/spreadsheets.readonly');
    authUrl.searchParams.set('code_challenge',challenge);
    authUrl.searchParams.set('code_challenge_method','S256');
    authUrl.searchParams.set('state',state);
    authUrl.searchParams.set('access_type','offline');
    authUrl.searchParams.set('prompt','consent');
    if(String(loginHint||'').includes('@')) authUrl.searchParams.set('login_hint',String(loginHint).trim());
    await shell.openExternal(authUrl.toString());
    timer=setTimeout(()=>{if(!settled){settled=true;callbackReject(googleOAuthError('Google 계정 승인이 시간 초과되었습니다.','UEP_GOOGLE_OAUTH_TIMEOUT'));}},180000);
    const code=await callbackPromise;
    const body=new URLSearchParams();
    body.set('client_id',clientId);

  
---
rams.set('access_type','offline');
    authUrl.searchParams.set('prompt','consent');
    if(String(loginHint||'').includes('@')) authUrl.searchParams.set('login_hint',String(loginHint).trim());
    await shell.openExternal(authUrl.toString());
    timer=setTimeout(()=>{if(!settled){settled=true;callbackReject(googleOAuthError('Google 계정 승인이 시간 초과되었습니다.','UEP_GOOGLE_OAUTH_TIMEOUT'));}},180000);
    const code=await callbackPromise;
    const body=new URLSearchParams();
    body.set('client_id',clientId);

    body.set('code',code);
    body.set('code_verifier',verifier);
    body.set('redirect_uri',redirectUri);
    body.set('grant_type','authorization_code');
    const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded;charset=UTF-8','accept':'application/json'},body:body.toString()});
    if(!response.ok){
      let errCode='',detail='',raw='';
      try{const payload=await response.json();errCode=String(payload?.error||'');detail=String(payload?.error_description||'');raw=JSON.stringify(payload);}catch{try{raw=await response.text();}catch{}}
      const suffix=[errCode,detail].filter(Boolean).join(' · ');
      throw googleOAuthError(`Google 토큰 교환 실패 (${response.status})${suffix?`: ${suffix}`:''}${!suffix&&raw?`: ${raw}`:''}`,'UEP_GOOGLE_OAUTH_TOKEN_FAILED');
    }
    const tokenPayload=await response.json();
    if(!tokenPayload.access_token) throw googleOAuthError('Go
---
=== MAIN TERM: client_secret ===
count=0
=== MAIN TERM: invalid_client ===
count=0
=== MAIN TERM: refresh_token ===
count=5

  try{
    const recovered=await readGoogleUserOAuthRecovery();
    if(recovered&&typeof recovered==='object'){await saveGoogleUserOAuth(recovered);return recovered;}
  }catch{}
  return null;
}
async function googleUserInfo(accessToken){
  const response=await net.fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{authorization:`Bearer ${accessToken}`}});
  if(!response.ok) throw googleOAuthError(`Google 사용자 확인 실패 (${response.status})`,'UEP_GOOGLE_OAUTH_USERINFO_FAILED');
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");
---
response.status})`,'UEP_GOOGLE_OAUTH_USERINFO_FAILED');
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(60,Number(next.expires_in||3600))*1000,updated_at:new Date().toISOString()};
  await saveGoogleUserOAuth(merged);
  return merged;
}
async function getGoogleUserSheetsToken(){
  let saved=await readGoogleUserOAuth();
  if(!saved) throw googleOAuthError("Google 계정 연결이 필요합니다.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  if(sa
---
GLE_OAUTH_USERINFO_FAILED');
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(60,Number(next.expires_in||3600))*1000,updated_at:new Date().toISOString()};
  await saveGoogleUserOAuth(merged);
  return merged;
}
async function getGoogleUserSheetsToken(){
  let saved=await readGoogleUserOAuth();
  if(!saved) throw googleOAuthError("Google 계정 연결이 필요합니다.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  if(saved.access_token && Number(
---
;
  return await response.json();
}
async function refreshGoogleUserOAuth(saved){
  if(!saved?.refresh_token) throw googleOAuthError("Google 계정 연결을 다시 승인해 주세요.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  const clientId=await loadGoogleOAuthClientId();
  if(String(saved.client_id||'').trim() && String(saved.client_id).trim()!==clientId){
    await disconnectGoogleUser();
    throw googleOAuthError('Google 연결 설정이 갱신되었습니다. Google 계정 연결을 한 번만 다시 승인해 주세요.','UEP_GOOGLE_USER_AUTH_REQUIRED');
  }
  const body=new URLSearchParams({client_id:clientId,refresh_token:String(saved.refresh_token),grant_type:'refresh_token'});
  const response=await net.fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body});
  if(!response.ok){let detail='';try{detail=(await response.json())?.error_description||'';}catch{};throw googleOAuthError(`Google 계정 토큰 갱신 실패 (${response.status})${detail?`: ${detail}`:''}`,"UEP_GOOGLE_USER_AUTH_REQUIRED");}
  const next=await response.json();
  const merged={...saved,...next,client_id:clientId,expires_at:Date.now()+Math.max(60,Number(next.expires_in||3600))*1000,updated_at:new Date().toISOString()};
  await saveGoogleUserOAuth(merged);
  return merged;
}
async function getGoogleUserSheetsToken(){
  let saved=await readGoogleUserOAuth();
  if(!saved) throw googleOAuthError("Google 계정 연결이 필요합니다.","UEP_GOOGLE_USER_AUTH_REQUIRED");
  if(saved.access_token && Number(saved.expires_at||0)>Date.n
---
nc (_event, programId) => {const target=programEvidencePath(programId);await fs.mkdir(target,{recursive:true});return {ok:!(await shell.openPath(target)),folder:target};});
  ipcMain.handle("google:credentialStatus", async () => {
  try {
    const credentials = await readEncrypted(credentialPath());
    if (validateServiceAccount(credentials)) return {ok:true,encryption:safeStorage.isEncryptionAvailable(),local:false,mode:"service_account",account:credentials.client_email||""};
  } catch (error) {}
  try {
    const userOAuth=await readGoogleUserOAuth();
    if(userOAuth && (userOAuth.refresh_token || (userOAuth.access_token && Number(userOAuth.expires_at||0)>Date.now()+60000))){
      return {ok:true,authorized:true,encryption:safeStorage.isEncryptionAvailable(),local:true,mode:"user_oauth",account:String(userOAuth.email||""),userOAuth:true,connectionState:"authorized",tokenPersisted:true};
    }
  } catch (error) {}
  return {ok:false,setupRequired:true,missing:true,userOAuth:true,connectionState:"not_authorized",reason:"담임용 Google 읽기 연결이 필요합니다. Google 계정 연결 버튼에서 1회 승인해 주세요."};
});
ipcMain.handle("google:authorizeUser", async (_event,payload={}) => {
  try{return await authorizeGoogleUser({loginHint:String(payload?.loginHint||payload?.email||"")});}
  catch(error){return {ok:false,reason:error?.message||String(error),code:error?.code||""};}
});
ipcMain.handle("google:diagnoseUserOAuth", async () => {
  const result={authorized:false,account:"",tokenOk:fal
---
=== APPROVAL JS TERM: approvalListMarkup ===
count=3
="readonly-rule"><b>연결 원본</b><span>2026. 운호고 1학년 야자 감독 / 석식 / 특근매식비 · 7월</span></div><p class="drawer-note">날짜 행과 다음 행의 감독교사를 짝지어 읽습니다. 원본이 비어 있으면 임의로 이름을 표시하지 않습니다.</p></div>`,
    ],
    "homeroom-timetable": [
      "HOMEROOM TIMETABLE",
      "담임반 시간표 · 1학년 1반",
      `<div class="connection-guide"><span>NEIS</span><h3>학급 시간표 조회</h3><p>실제 NEIS 자료를 불러오려면 시간표 메뉴의 학급 시간표 조회를 이용하세요.</p></div>`,
    ],
  };
  const data = refs[type];
  if (!data) return;
  const [k, t, b] = data;
  $("#drawerKicker").textContent = k;
  $("#drawerTitle").textContent = t;
  $("#drawerBody").innerHTML = b;
  $("#drawer").classList.remove("hidden");
  $("#drawerBackdrop").classList.remove("hidden");
}


function approvalListMarkup(rows) {
  if (!rows.length) return '<div class="outing-empty">검색 결과가 없습니다.</div>';
  return rows.map((row) => {
    const index = SOURCE_REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButt
---
  const meta = {
    approval: ["APPROVAL LINE", "주요 업무 결재라인"],
    homeroom: ["HOMEROOM", "담임·부담임 명단"],
    extension: ["DIRECTORY & SEATING", "내선번호·자리배치"],
  }[type];
  const rows = SOURCE_REFERENCE_DATA[type] || [];
  $("#drawerKicker").textContent = meta[0];
  $("#drawerTitle").textContent = meta[1];
  $("#drawer").classList.remove("reference-expanded", "reference-maximized", "extension-expanded");
  if (type === "approval") {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="exte
---
(type === "approval") {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인터넷전화 내선번호 자리배치도"></div></section>`;
    $("#drawer").classList.add("extension-expanded");
    bindExtensionReference();
  } else {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="학년·반·담임·부담임 검색"><span>${rows.length}건</span></div><div id="sourceReferenc
---
=== APPROVAL CSS TERM: approvalListMarkup ===
count=0
=== APPROVAL JS TERM: showApprovalDetail ===
count=5
div>`,
    ],
  };
  const data = refs[type];
  if (!data) return;
  const [k, t, b] = data;
  $("#drawerKicker").textContent = k;
  $("#drawerTitle").textContent = t;
  $("#drawerBody").innerHTML = b;
  $("#drawer").classList.remove("hidden");
  $("#drawerBackdrop").classList.remove("hidden");
}


function approvalListMarkup(rows) {
  if (!rows.length) return '<div class="outing-empty">검색 결과가 없습니다.</div>';
  return rows.map((row) => {
    const index = SOURCE_REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButton = $("[data-extension-tab='table']");
  const imagePanel = $("#extensionImagePanel");
  const tablePanel = $("#extensionTablePanel");
  const switchTab = (name) => {
    imageButton?.classList.toggle("active", name === "image");
    tableButton?.classList.toggle("active", name === "table");
    imagePanel?.classList.toggle("hidden", name !== "image");
    tablePanel?.classList.toggle("h
---
nceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인터넷전화 내선번호 자리배치도"></div></section>`;
    $("#drawer").classList.add("extension-expanded");
    bindExtensionReference();
  } else {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="학년·반·담임·부담임 검색"><span>${rows.length}건</span></div><div id="sourceReferenceRows">${sourceReferenceRowsMarkup(rows, type)}</div><p class="drawer-note">2026년 담임교사 명단(부담임 포함) 기준 · 읽기 전용</p>`;
    $("#drawer").classLis
---
lass="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인터넷전화 내선번호 자리배치도"></div></section>`;
    $("#drawer").classList.add("extension-expanded");
    bindExtensionReference();
  } else {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="학년·반·담임·부담임 검색"><span>${rows.length}건</span></div><div id="sourceReferenceRows">${sourceReferenceRowsMarkup(rows, type)}</div><p class="drawer-note">2026년 담임교사 명단(부담임 포함) 기준 · 읽기 전용</p>`;
    $("#drawer").classList.add("reference-expanded");
    const bindHomeroomIfNeeded=()=>{if(type==="ho
---
").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인터넷전화 내선번호 자리배치도"></div></section>`;
    $("#drawer").classList.add("extension-expanded");
    bindExtensionReference();
  } else {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="학년·반·담임·부담임 검색"><span>${rows.length}건</span></div><div id="sourceReferenceRows">${sourceReferenceRowsMarkup(rows, type)}</div><p class="drawer-note">2026년 담임교사 명단(부담임 포함) 기준 · 읽기 전용</p>`;
    $("#drawer").classList.add("reference-expanded");
    const bindHomeroomIfNeeded=()=>{if(type==="homeroom")bindHomeroomTeacherLinks();};
    bindHomeroomIfNeeded();
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase
---
ReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인터넷전화 내선번호 자리배치도"></div></section>`;
    $("#drawer").classList.add("extension-expanded");
    bindExtensionReference();
  } else {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="학년·반·담임·부담임 검색"><span>${rows.length}건</span></div><div id="sourceReferenceRows">${sourceReferenceRowsMarkup(rows, type)}</div><p class="drawer-note">2026년 담임교사 명단(부담임 포함) 기준 · 읽기 전용</p>`;
    $("#drawer").classList.add("reference-expanded");
    const bindHomeroomIfNeeded=()=>{if(type==="homeroom")bindHomeroomTeacherLinks();};
    bindHomeroomIfNeeded();
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.joi
---
=== APPROVAL CSS TERM: showApprovalDetail ===
count=0
=== APPROVAL JS TERM: approval-explorer ===
count=1
homeroomTeacher||"");}));
}
function openSourceReference(type) {
  const meta = {
    approval: ["APPROVAL LINE", "주요 업무 결재라인"],
    homeroom: ["HOMEROOM", "담임·부담임 명단"],
    extension: ["DIRECTORY & SEATING", "내선번호·자리배치"],
  }[type];
  const rows = SOURCE_REFERENCE_DATA[type] || [];
  $("#drawerKicker").textContent = meta[0];
  $("#drawerTitle").textContent = meta[1];
  $("#drawer").classList.remove("reference-expanded", "reference-maximized", "extension-expanded");
  if (type === "approval") {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extens
---
=== APPROVAL CSS TERM: approval-explorer ===
count=1
!important;grid-template-columns:78px minmax(0,1fr) auto!important;align-items:start!important;gap:10px!important;height:auto!important;min-height:0!important}
.duty-calendar-meal-list .duty-meal-menu-full{display:flex!important;flex-wrap:wrap!important;gap:5px 10px!important;white-space:normal!important;overflow:visible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-di
---
=== APPROVAL JS TERM: approval-nav ===
count=3
BLE",
      "담임반 시간표 · 1학년 1반",
      `<div class="connection-guide"><span>NEIS</span><h3>학급 시간표 조회</h3><p>실제 NEIS 자료를 불러오려면 시간표 메뉴의 학급 시간표 조회를 이용하세요.</p></div>`,
    ],
  };
  const data = refs[type];
  if (!data) return;
  const [k, t, b] = data;
  $("#drawerKicker").textContent = k;
  $("#drawerTitle").textContent = t;
  $("#drawerBody").innerHTML = b;
  $("#drawer").classList.remove("hidden");
  $("#drawerBackdrop").classList.remove("hidden");
}


function approvalListMarkup(rows) {
  if (!rows.length) return '<div class="outing-empty">검색 결과가 없습니다.</div>';
  return rows.map((row) => {
    const index = SOURCE_REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButton = $("[data-extension-tab='table']");
  const imagePanel = $("#extensionImagePanel");
  const tablePanel = $("#extensionTablePanel");
  const switchTab = (name) => {
    imageButton?.classList.toggle("active", name === "image");
    
---
REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButton = $("[data-extension-tab='table']");
  const imagePanel = $("#extensionImagePanel");
  const tablePanel = $("#extensionTablePanel");
  const switchTab = (name) => {
    imageButton?.classList.toggle("active", name === "image");
    tableButton?.classList.toggle("active", name === "table");
    imagePanel?.classList.toggle("hidden", name !== "image");
    tablePanel?.classList.toggle("hidden", name !== "table");
  };
  if (imageButton) imageButton.onclick = () => switchTab("image");
  if (tableButton) tableButton.onclick = () => switchTab("table");
  const image = $("#extensionLayoutImage");
  if (image) image.onclick = () => { const zoomed = image.classList.toggle("zoomed"); image.closest(".extension-image-scroll")?.classList.toggle("zoom-mode", zoomed); };
  const search = $("#sourceReferenceSearch");
  if (search) search.oninput = () => {

---
ference(type) {
  const meta = {
    approval: ["APPROVAL LINE", "주요 업무 결재라인"],
    homeroom: ["HOMEROOM", "담임·부담임 명단"],
    extension: ["DIRECTORY & SEATING", "내선번호·자리배치"],
  }[type];
  const rows = SOURCE_REFERENCE_DATA[type] || [];
  $("#drawerKicker").textContent = meta[0];
  $("#drawerTitle").textContent = meta[1];
  $("#drawer").classList.remove("reference-expanded", "reference-maximized", "extension-expanded");
  if (type === "approval") {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scrol
---
=== APPROVAL CSS TERM: approval-nav ===
count=4
ght:auto!important;min-height:0!important}
.duty-calendar-meal-list .duty-meal-menu-full{display:flex!important;flex-wrap:wrap!important;gap:5px 10px!important;white-space:normal!important;overflow:visible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-out
---
meal-menu-full{display:flex!important;flex-wrap:wrap!important;gap:5px 10px!important;white-space:normal!important;overflow:visible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{g
---
isible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{gri
---
alendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
=== APPROVAL JS TERM: approval-nav-row ===
count=2
BLE",
      "담임반 시간표 · 1학년 1반",
      `<div class="connection-guide"><span>NEIS</span><h3>학급 시간표 조회</h3><p>실제 NEIS 자료를 불러오려면 시간표 메뉴의 학급 시간표 조회를 이용하세요.</p></div>`,
    ],
  };
  const data = refs[type];
  if (!data) return;
  const [k, t, b] = data;
  $("#drawerKicker").textContent = k;
  $("#drawerTitle").textContent = t;
  $("#drawerBody").innerHTML = b;
  $("#drawer").classList.remove("hidden");
  $("#drawerBackdrop").classList.remove("hidden");
}


function approvalListMarkup(rows) {
  if (!rows.length) return '<div class="outing-empty">검색 결과가 없습니다.</div>';
  return rows.map((row) => {
    const index = SOURCE_REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButton = $("[data-extension-tab='table']");
  const imagePanel = $("#extensionImagePanel");
  const tablePanel = $("#extensionTablePanel");
  const switchTab = (name) => {
    imageButton?.classList.toggle("active", name === "image");
    
---
REFERENCE_DATA.approval.indexOf(row);
    return `<button type="button" class="approval-nav-row" data-approval-index="${index}"><b>${escapeHtml(row[0])}</b><span>${escapeHtml(row[1] || "")}</span></button>`;
  }).join("");
}

function showApprovalDetail(index) {
  const row = SOURCE_REFERENCE_DATA.approval[Number(index)] || SOURCE_REFERENCE_DATA.approval[0];
  const target = $("#approvalDetail");
  if (!target || !row) return;
  target.innerHTML = `<small>업무 분류</small><h3>${escapeHtml(row[0])}</h3><section><b>결재 순서</b><p>${escapeHtml(row[1] || "-")}</p></section><section><b>확인 사항</b><p>${escapeHtml(row[2] || "별도 비고 없음")}</p></section><footer>2026년 학교 주요 업무 결재 라인 기준 · 읽기 전용</footer>`;
  $$(".approval-nav-row").forEach((button) => button.classList.toggle("active", Number(button.dataset.approvalIndex) === Number(index)));
}

function bindExtensionReference() {
  const imageButton = $("[data-extension-tab='image']");
  const tableButton = $("[data-extension-tab='table']");
  const imagePanel = $("#extensionImagePanel");
  const tablePanel = $("#extensionTablePanel");
  const switchTab = (name) => {
    imageButton?.classList.toggle("active", name === "image");
    tableButton?.classList.toggle("active", name === "table");
    imagePanel?.classList.toggle("hidden", name !== "image");
    tablePanel?.classList.toggle("hidden", name !== "table");
  };
  if (imageButton) imageButton.onclick = () => switchTab("image");
  if (tableButton) tableButton.onclick = () => switchTab("table");
  const image = $("#extensionLayoutImage");
  if (image) image.onclick = () => { const zoomed = image.classList.toggle("zoomed"); image.closest(".extension-image-scroll")?.classList.toggle("zoom-mode", zoomed); };
  const search = $("#sourceReferenceSearch");
  if (search) search.oninput = () => {

---
=== APPROVAL CSS TERM: approval-nav-row ===
count=3
meal-menu-full{display:flex!important;flex-wrap:wrap!important;gap:5px 10px!important;white-space:normal!important;overflow:visible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{g
---
isible!important;text-overflow:clip!important;line-height:1.55!important}
.duty-calendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{gri
---
alendar-meal-list .duty-meal-menu-full i{font-style:normal!important;white-space:normal!important}
.approval-explorer{min-height:0!important;grid-template-columns:minmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
=== APPROVAL JS TERM: approval-detail ===
count=1
 homeroom: ["HOMEROOM", "담임·부담임 명단"],
    extension: ["DIRECTORY & SEATING", "내선번호·자리배치"],
  }[type];
  const rows = SOURCE_REFERENCE_DATA[type] || [];
  $("#drawerKicker").textContent = meta[0];
  $("#drawerTitle").textContent = meta[1];
  $("#drawer").classList.remove("reference-expanded", "reference-maximized", "extension-expanded");
  if (type === "approval") {
    $("#drawerBody").innerHTML = `<div class="reference-editor-tools source-reference-tools"><input id="sourceReferenceSearch" placeholder="업무 종류·결재선 검색"><span>${rows.length}건</span></div><div class="approval-explorer"><div id="approvalList" class="approval-nav">${approvalListMarkup(rows)}</div><article id="approvalDetail" class="approval-detail"></article></div>`;
    $("#drawer").classList.add("reference-maximized");
    $("#sourceReferenceSearch").oninput = () => {
      const query = $("#sourceReferenceSearch").value.trim().toLowerCase();
      const filtered = rows.filter((row) => row.join(" ").toLowerCase().includes(query));
      $("#approvalList").innerHTML = approvalListMarkup(filtered);
      $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
      if (filtered.length) showApprovalDetail(SOURCE_REFERENCE_DATA.approval.indexOf(filtered[0]));
    };
    $("#approvalList").querySelectorAll("[data-approval-index]").forEach((button) => button.onclick = () => showApprovalDetail(button.dataset.approvalIndex));
    showApprovalDetail(0);
  } else if (type === "extension") {
    $("#drawerBody").innerHTML = `<section id="extensionImagePanel" class="extension-image-panel extension-image-only"><div class="extension-image-scroll"><img id="extensionLayoutImage" src="./assets/extension-layout.png" alt="운호고등학교 인
---
=== APPROVAL CSS TERM: approval-detail ===
count=4
inmax(280px,.72fr) minmax(420px,1.28fr)!important}
.approval-nav{gap:4px!important;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
;max-height:calc(100vh - 185px)!important}
.approval-nav-row{gap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
ap:2px!important;padding:8px 11px!important;border-radius:10px!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
!important;min-height:0!important}
#drawer .approval-nav-row b{font-size:14px!important;line-height:1.25!important}
#drawer .approval-nav-row span{font-size:11px!important;line-height:1.35!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}
.approval-detail{gap:8px!important;min-height:0!important;padding:17px!important}
.approval-detail section{padding:11px 13px!important}
#drawer .approval-detail section b{font-size:13px!important}
#drawer .approval-detail section p{margin:5px 0 0!important;font-size:15px!important;line-height:1.55!important}
.dorm-outing-teacher-dialog{max-width:760px!important}.dorm-outing-editor-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px}.dorm-outing-editor-grid label{display:grid;gap:6px}.dorm-outing-editor-grid .wide{grid-column:1/-1}@media(max-width:760px){.dorm-outing-editor-grid{grid-template-columns:1fr}.dorm-outing-editor-grid .wide{grid-column:auto}}
---
