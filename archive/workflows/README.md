# 보관된 옛 워크플로

2026-09-19에 `.github/workflows/`에서 옮겨 둔 버전별 일회성 워크플로 438개입니다 (진단·검사·패치·빌드·릴리스).
이 폴더에 있으면 GitHub Actions가 실행하지 않습니다. 기록용이며, 현재 배포는 `.github/workflows/release.yml` / `rollback.yml` 만 사용합니다.
`build-homeroom-installer.yml`(최초 설치용 런처 설치파일 빌드)은 그대로 남겨 두었습니다.
