name: Inspect UEP 0.82.62 Admission Range
on: {push: {paths: ['inspect-08262.trigger']}}
permissions: {contents: read}
jobs:
  inspect:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: pwsh
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          $ErrorActionPreference='Stop'
          New-Item -ItemType Directory -Force source,app|Out-Null
          gh release download v0.82.61 --repo $env:GITHUB_REPOSITORY --pattern UEP-update.zip --dir source
          if($LASTEXITCODE -ne 0){throw 'download failed'}
          Expand-Archive source/UEP-update.zip app -Force
          node scripts/inspect-uep-08262-admission-range.js app/resources/app | Set-Content inspect-08262-range.txt -Encoding utf8
      - uses: actions/upload-artifact@v4
        with:
          name: inspect-08262-range
          path: inspect-08262-range.txt
