name: Legacy UEP release workflow (manual only)
'on':
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy duplicate release workflow is disabled; use build-uep-0.81.72-performance-candidate-r2.yml."
